# 发布日志

## v1.1.0（2026-09-21）

### 新增

- **定时自动同步**：默认每 30 分钟同步一次，间隔可配置（`AutoRefreshMinutes`，`0` 关闭）
  - 间隔下限夹到 5 分钟，避免频繁请求雨课堂被风控
  - 单次失败不终止循环：网络抖动与登录态临时失效都能自愈
- **新作业系统通知**：发现新作业时发 Windows toast，点击直达该课程的雨课堂页面
  - 首次同步只建立基线，**不会把已有作业全报一遍**
  - 按稳定 Id 判定新增（`hw-{课堂}-{叶子}`），改标题不误报；已完成的作业不算新待办
  - 一次发现多项合并成一条通知，避免刷屏
  - 未打包 Win32 应用需要「自注册 AUMID + 开始菜单快捷方式」才能发通知，均已实现，且无需管理员权限
- **桌面小组件**：仿滴答清单的桌面挂件
  - 无边框透明玻璃面板，普通窗口层级（不置顶；从托盘/主界面召唤时会到最前）
  - 左键拖动移动位置、拖边缘/角落调整大小，位置与尺寸自动记忆
  - 复用主界面缓存，不额外请求网络
  - 分组切换（我的一天 / 作业 / 计划内 / 所有），To Do 风格图标列表
  - 头部带同步按钮（同步中图标旋转，页脚同步进度与主界面同源）
  - 浅色/深色跟随主程序
- **待办本地缓存（todos.json）**：启动先用上次同步的列表秒开首屏，网络同步在后台追平；
  状态栏在同步完成前显示缓存提示；退出登录时缓存一并清除
- **增量同步**：每门课按作业日志签名（id/标题/发布时间/截止时间/leaf_id）比对，
  未变化的课程跳过章节树与逐条详情请求（请求数大头），只拉一次完成进度校验已有作业的状态；
  缓存超过 24 小时强制全量校验，兜住「日志不动」的服务端变更
- **缓存合法性校验**：加载时丢弃缺字段/前缀不符/时间戳离谱/重复 id 的坏条目；
  同步时课程消失、作业被删等失效项自动剔除
- **自绘托盘菜单**：替代无法定制样式的系统原生菜单，Win11 风格圆角面板，
  带勾选态，随主题切换；含「显示桌面小组件」「立即同步一次」

### 修复

- 小组件拖动失效（手算坐标的振荡 bug，改走 OS 级拖窗）
- 小组件勾选框点击无响应、退出登录后小组件不刷新
- 系统通知链路两处 ABI 错误（WinRT vtable 槽位、AUMID 的 PROPERTYKEY），
  此前通知实际从未真正发出
- 主窗口「退出」后点托盘图标崩溃（Cannot re-show a closed window）
- 进入主界面后状态栏仍显示「仍在等待扫码…」：登录页的后台自动检测在会话恢复/接管后
  没有被取消，僵尸轮询继续写状态栏
- 自动同步在后台线程更新界面状态、登录后自动同步不启动、登出后不同步停止
- 「新作业通知」设置项此前不生效

### 打包

- 推荐分发 `trim` 裁剪单文件：**40.7 MB**（v1.0.0 的 `single` 完整单文件约 100 MB，-60%）
- 发布命令：`.\tools\publish.ps1 -Mode trim -NoPdb`，产物在 `publish\trim\`

### 质量保障

- 新增 `--toastcheck`：逐项自检通知链路（AUMID 注册 → 快捷方式 → XML 转义 → WinRT 投递），失败时给出 HRESULT
- 新增 `--selfcheck-widget`：小组件离屏渲染与绑定自检
- `--jsoncheck` 增补「新作业判定」与「待办缓存往返与校验」用例，
  并在裁剪产物上全量通过（裁剪版必过项）
- `--logintest` 登录链路回归通过

### 说明

- 系统通知在**没有交互式桌面的会话**（会话隔离、通知服务未运行等）中无法投递，
  `CreateToastNotifier` 返回 `0x80070490`。此时应用其余功能不受影响；
  `--toastcheck` 会明确区分「环境不支持通知」与「配置错误」，不会让人误以为代码有问题。

## v1.0.0（2026-09-17）

首个正式发布的版本。

### 功能

- **作业清单**：自动同步长江雨课堂的作业与公告，按课程归集，显示截止时间与剩余天数，逾期标红
- **真实完成状态**：进度来自雨课堂作答记录，而非按截止时间猜测
- **公告已读管理**：已读/未读分组，支持单条已读与全部已读
- **双登录方式**：内嵌官方登录页微信扫码；无 WebView2 时可粘贴 Cookie 登录
- **Fluent 半透明界面**：亚克力磨砂背景，支持自定义壁纸与透明度滑块
- **深色模式**：浅色 / 深色 / 跟随系统，即时切换
- **托盘常驻**：关闭窗口时可选「直接退出」或「最小化运行」，托盘图标一键还原
- **自绘程序图标**：窗口、任务栏、托盘、exe 统一图标
- **登录态安全**：`session.json` 使用 Windows DPAPI 加密，仅本机当前用户可解
- **学期可配置**：编辑 `settings.json` 切换学期/学校，无需改代码

### 界面

- 三栏布局（导航 / 待办列表 / 详情），仿 Microsoft To Do 视觉语言
- 微软风登录页（480×700，与二维码页面尺寸相当），居中对齐
- 弹窗全面主题化，深浅色一致

### 稳定性修复

- 修复登录后同步在裁剪发布下崩溃的问题：全部 JSON 序列化改走源生成上下文，不再依赖反射
- 修复「扫码成功却卡在登录页」的多个竞态（WebView 线程亲和性、同步重入、忙碌计数）
- 修复退出登录后 WebView 仍残留登录态的问题（退出时清空雨课堂域 Cookie）
- 修复列表勾选框对勾偏移
- 登录态文件损坏时按未登录处理，不再导致启动失败

### 打包

| 模式 | 大小 | 说明 |
|---|---|---|
| `trim` 裁剪单文件 | **40.8 MB** | 推荐分发 |
| `single` 完整单文件 | 约 100 MB | 兼容性最好 |
| `fdd` 框架依赖 | 45.9 MB | 目标机需装 .NET 10 运行时 |
| `sc` 自包含多文件 | 107.1 MB | — |

发布命令：`.\tools\publish.ps1 -Mode <模式> -NoPdb`，产物在 `publish\<模式>\`。
分发前请删除产物目录中的 `.WebView2` 文件夹（本机登录 cookie 缓存）。

### 质量保障

- `--selfcheck`：窗口/绑定自检 + 离屏渲染，退出码 0 为通过
- `--jsoncheck`：全部序列化路径往返自检（本版本新增，裁剪包必过项）
- `--webtest` / `--logintest` / `--cookietest`：登录链路回归测试
- `tools\SyncSmoke`：无界面真实同步冒烟

### 运行要求

- Windows 10/11 x64
- WebView2 运行时（多数系统已自带；缺失时可用 Cookie 登录兜底）
